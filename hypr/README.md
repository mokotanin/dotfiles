all of this is pretty much the auto generated config but split in multiples files for better organization and readability.

in the [workspace](./workspaces.lua) folder you will find the workspace configuration for linktui
in [visual](./visual.lua) folder you will find the visual configuration for a non-used l1p0-menus (at least for now).

dependency for the [hyprpaper.conf](./hyprpaper.conf) is itself i think you can understand xD