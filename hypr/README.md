Everything was taken from the auto-generated hyprland config, and then split into multiple files for better organization and readability.

In the [workspace](./workspaces.lua):69 file, you will find the window rule for a nice use of linktui with Waybar.
In [visual](./visual.lua):120 file, you will find the visual configuration for a non-used l1p0-menus (at least for now).

Dependency for the [hyprpaper.conf](./hyprpaper.conf):
```
sudo pacman -S hyprpaper
```
